@page "/admin/animes/create"
@page "/admin/animes/update/{Id:int}"
@using AnimeHub.Shared.Models.Dtos.Anime
@using AnimeHub.Shared.Models.Dtos.Category
@using AnimeHub.Shared.Models.Dtos.Genre
@using AnimeHub.Shared.Models.Dtos.Studio
@using AnimeHub.Shared.Models.Enums
@using System.ComponentModel.DataAnnotations
@using AnimeHubClient.Services
@inject NavigationManager Navigation
@inject ISnackbar Snackbar
@inject IAnimeService AnimeService
@inject ILookUpService LookUpService

@if (IsLoading)
{
    <div class="py-4 text-center"><MudProgressCircular Indeterminate="true" /></div>
}
else
{
    <MudCard Elevation="4" Class="mt-4">
        <MudCardHeader>
            <CardHeaderContent>
                <MudText Align="Align.Center" Typo="Typo.h4" Class="fw-bold">@(Id > 0 ? "Update" : "Create") Anime</MudText>
            </CardHeaderContent>
        </MudCardHeader>
        <MudCardContent>
            <EditForm Model="Anime" FormName="AnimeUpsertForm" OnValidSubmit="UpsertAnime">
                <DataAnnotationsValidator />

                <MudTextField Variant="Variant.Outlined" Label="Anime Title" @bind-Value="Anime.Title" For="@(() => Anime.Title)" Class="my-3" />
                <MudTextField Variant="Variant.Outlined" Label="Description" Lines="3" @bind-Value="Anime.Description" For="@(() => Anime.Description)" Class="my-3" />

                <MudGrid>
                    <MudItem xs="12" sm="6">
                        <MudNumericField T="double?" Variant="Variant.Outlined" Label="Rating" @bind-Value="Anime.Rating" Min="0.0" Max="10.0" For="@(() => Anime.Rating)" Class="my-3" />
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudNumericField T="int?" Variant="Variant.Outlined" Label="Episodes" @bind-Value="Anime.Episodes" For="@(() => Anime.Episodes)" Class="my-3" />
                    </MudItem>
                </MudGrid>

                <MudGrid>
                    <MudItem xs="12" sm="6">
                        <MudSelect T="int?" Label="Season" Variant="Variant.Outlined" @bind-Value="Anime.Season" For="@(() => Anime.Season)" Class="my-3">
                            @foreach (var seasonValue in Enum.GetValues(typeof(Season)))
                            {
                                <MudSelectItem T="int?" Value="@((int)seasonValue)">@seasonValue.ToString()</MudSelectItem>
                            }
                        </MudSelect>
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudNumericField T="int?" Variant="Variant.Outlined" Label="Premiered Year" Min="1900" Max="2100" @bind-Value="Anime.PremieredYear" For="@(() => Anime.PremieredYear)" Class="my-3" />
                    </MudItem>
                </MudGrid>

                <MudSelect T="Status?" Label="Status" Variant="Variant.Outlined" @bind-Value="@AnimeStatusEnum" Class="my-3">
                    @foreach (Status statusValue in Enum.GetValues(typeof(Status)))
                    {
                        <MudSelectItem T="Status?" Value="@statusValue">@GetEnumDisplayName(statusValue)</MudSelectItem>
                    }
                </MudSelect>

                <MudSelect T="int?" Label="Category" Variant="Variant.Outlined" @bind-Value="Anime.CategoryId" For="@(() => Anime.CategoryId)" Class="my-3">
                    @foreach (var category in Categories)
                    {
                        <MudSelectItem T="int?" Value="@category.Id">@category.Name</MudSelectItem>
                    }
                </MudSelect>

                <MudSelect T="int"
                           Label="Studios"
                           Variant="Variant.Outlined"
                           SelectedValues="Anime.StudioIds"
                           SelectedValuesChanged="@(vals => Anime.StudioIds = new HashSet<int>(vals))"
                           MultiSelection="true"
                           Clearable="true"
                           ToStringFunc="@(new Func<int, string>(ConvertStudioIdToName))"
                           Class="my-3">

                    @if (Studios != null)
                    {
                        @foreach (var studio in Studios)
                        {
                            <MudSelectItem T="int" Value="@studio.Id">@studio.Name</MudSelectItem>
                        }
                    }
                </MudSelect>

                <MudPaper Elevation="2" Class="p-3 mt-3">
                    <MudText Typo="Typo.subtitle1" Class="mb-2">Genres</MudText>
                    <MudGrid>
                        @if (Genres != null)
                        {
                            @foreach (var genre in Genres)
                            {
                                <MudItem xs="6" sm="4" md="3" lg="2">
                                    <MudCheckBox onchange="e => ToggleGenre(genre.Id, e)"
                                                 T="bool"
                                                 @key="genre.Id"
                                                 Label="@genre.Name"
                                                 Value="@Anime.GenreIds.Contains(genre.Id)"
                                                 ValueChanged="e => ToggleGenre(genre.Id, e)" />
                                </MudItem>
                            }
                        }
                    </MudGrid>
                </MudPaper>

                <MudFileUpload T="IBrowserFile" OnFilesChanged="OnFileSelected" Class="mt-3">
                    <ActivatorContent>
                        <MudButton Variant="Variant.Filled"
                                   Color="Color.Primary"
                                   StartIcon="@Icons.Material.Filled.CloudUpload">
                            Upload Image
                        </MudButton>
                    </ActivatorContent>
                </MudFileUpload>

                @if (!string.IsNullOrEmpty(PreviewImageUrl) || !string.IsNullOrEmpty(UploadedImageUrl))
                {
                    <MudImage Src="@(string.IsNullOrEmpty(PreviewImageUrl) ? UploadedImageUrl : PreviewImageUrl)"
                              Alt="Anime Image"
                              Elevation="25"
                              Class="rounded-lg ma-4 d-block mx-auto"
                              Style="max-height:200px; max-width: 100%;" />
                }

                @* TRAILER & MEDIA *@
                <MudText Typo="Typo.h6" Class="mt-5">Trailer & Media</MudText>
                <MudDivider Class="my-2"/>
                
                <MudGrid>
                    @* Trailer Poster *@
                    <MudItem xs="12" md="6">
                        <MudFileUpload T="IBrowserFile" Accept=".jpg,.jpeg,.png" FilesChanged="HandlePosterUpload">
                            <ActivatorContent>
                                <MudButton HtmlTag="label"
                                           Variant="Variant.Filled"
                                           Color="Color.Secondary"
                                           StartIcon="@Icons.Material.Filled.Image">
                                    Upload Trailer Thumbnail
                                </MudButton>
                            </ActivatorContent>
                        </MudFileUpload>
                        
                        @if (IsUploadingPoster)
                        {
                            <MudProgressLinear Color="Color.Secondary" Indeterminate="true" Class="mt-2" />
                        }
                        
                        @if (!string.IsNullOrEmpty(Anime.TrailerPosterUrl))
                        {
                            <MudText Typo="Typo.caption" Class="d-block mt-1">Poster Uploaded</MudText>
                            <MudImage Src="@Anime.TrailerPosterUrl" 
                                      Height="100" Class="rounded mt-2" Style="object-fit:cover; aspect-ratio: 16/9;" />
                        }
                    </MudItem>

                    @* Trailer Video (MP4) *@
                    <MudItem xs="12" md="6">
                        <MudFileUpload T="IBrowserFile" Accept=".mp4" FilesChanged="HandleTrailerUpload">
                            <ActivatorContent>
                                <MudButton HtmlTag="label"
                                           Variant="Variant.Filled"
                                           Color="Color.Error"
                                           StartIcon="@Icons.Material.Filled.Movie">
                                    Upload Trailer Video
                                </MudButton>
                            </ActivatorContent>
                        </MudFileUpload>

                        @if (IsUploadingTrailer)
                        {
                            <MudProgressLinear Color="Color.Error" Indeterminate="true" Class="mt-2" />
                            <MudText Typo="Typo.caption">Uploading Video...</MudText>
                        }

                        @if (!string.IsNullOrEmpty(Anime.TrailerUrl))
                        {
                            <MudText Typo="Typo.caption" Class="d-block mt-1">Video Uploaded</MudText>
                            <MudText Typo="Typo.body2" Color="Color.Success">@Anime.TrailerUrl</MudText>
                        }
                    </MudItem>
                </MudGrid>

                <MudCardActions Class="py-4">
                    <MudGrid Justify="Justify.Center" Class="px-2">
                        <MudItem xs="12" sm="6">
                            <MudButton ButtonType="ButtonType.Submit"
                                       StartIcon="@Icons.Material.Filled.LibraryAddCheck"
                                       Variant="Variant.Filled"
                                       Color="Color.Primary"
                                       FullWidth="true"
                                       Disabled="@IsLoading">
                                @(Id > 0 ? "Update" : "Create")
                            </MudButton>
                        </MudItem>
                        <MudItem xs="12" sm="6">
                            <MudButton Href="/admin/animes"
                                       StartIcon="@Icons.Material.Filled.ArrowCircleLeft"
                                       Variant="Variant.Filled"
                                       Color="Color.Default"
                                       FullWidth="true"
                                       Disabled="@IsLoading">
                                Back to List
                            </MudButton>
                        </MudItem>
                    </MudGrid>
                </MudCardActions>
            </EditForm>
        </MudCardContent>
    </MudCard>
}

@code {
    [Parameter]
    public int Id { get; set; }

    // Single form model, can be either Create or Update DTO
    private AnimeUpdateDto Anime { get; set; } = new();

    private List<CategoryReadDto>? Categories { get; set; } = new();
    private List<GenreReadDto>? Genres { get; set; } = new();
    private List<StudioReadDto>? Studios { get; set; } = new();

    private bool IsLoading { get; set; } = true;
    // Upload State Flags
    private bool IsUploadingPoster { get; set; } = false;
    private bool IsUploadingTrailer { get; set; } = false;

    private IBrowserFile? SelectedFile;
    private string? PreviewImageUrl;   // Base64 local preview
    private string? UploadedImageUrl;  // Server path from API

    protected override async Task OnParametersSetAsync()
    {
        await LoadAnime();
        IsLoading = false;
        StateHasChanged();
    }

    private async Task LoadAnime()
    {
        if (Id > 0)
        {
            var result = await AnimeService.GetAnimeByIdAsync(Id);
            if (result != null)
            {
                Anime.Id = result.Id;
                Anime.Title = result.Title;
                Anime.Episodes = result.Episodes;
                Anime.Season = (int)Enum.Parse(typeof(Season), result.Season);
                Anime.PremieredYear = result.PremieredYear;
                Anime.Description = result.Description;
                Anime.ImageUrl = result.ImageUrl;
                Anime.TrailerPosterUrl = result.TrailerPosterUrl;
                Anime.TrailerUrl = result.TrailerUrl;
                Anime.Rating = result.Rating;
                Anime.Status = (int)Enum.Parse(typeof(Status), result.Status);
                Anime.CategoryId = result.CategoryId;
                Anime.GenreIds = result.GenreIds ?? new List<int>();
                Anime.StudioIds = result.StudioIds ?? new HashSet<int>();

                // Ensure the path uses forward slashes for web access
                UploadedImageUrl = result.ImageUrl?.Replace("\\", "/");
            }
        }
        else // Handle case for new anime creation (Id == 0)
        {
            // Reset the Anime object and image URLs for a new entry
            Anime = new AnimeUpdateDto
            {
                GenreIds = new List<int>(),
                StudioIds = new HashSet<int>(),
                // Set status to null, so the MudSelect appears unselected.
                Status = null
            };
            PreviewImageUrl = null;
            UploadedImageUrl = null;
        }

        var categories = LookUpService.GetCategoriesAsync();
        var genres = LookUpService.GetGenresAsync();
        var studios = LookUpService.GetStudiosAsync();

        await Task.WhenAll(categories, genres, studios);

        // Assign results (ensure they are not null)
        Categories = categories.Result?.OrderBy(c => c.Name).ToList() ?? new();
        Genres = genres.Result?.OrderBy(g => g.Name).ToList() ?? new();
        Studios = studios.Result?.OrderBy(s => s.Name).ToList() ?? new();
    }

    // Trailer file handlers
    private async Task HandlePosterUpload(IBrowserFile file)
    {
        if (file.Size > 5 * 1024 * 1024)
        {
            Snackbar.Add("Poster file too large.", Severity.Warning);
            return;
        }

        IsUploadingPoster = true;
        StateHasChanged();

        var path = await AnimeService.UploadTrailerPosterAsync(file);

        if (!string.IsNullOrEmpty(path))
        {
            Anime.TrailerPosterUrl = path; // Update DTO
            Snackbar.Add("Trailer poster uploaded!", Severity.Success);
        }
        else
        {
            Snackbar.Add("Trailer poster upload failed.", Severity.Error);
        }

        IsUploadingPoster = false;
        StateHasChanged();
    }

    private async Task HandleTrailerUpload(IBrowserFile file)
    {
        if (file.Size > 100 * 1024 * 1024)
        {
            Snackbar.Add("Video file too large (Max 100MB).", Severity.Warning);
            return;
        }

        IsUploadingTrailer = true;
        StateHasChanged();

        var path = await AnimeService.UploadTrailerAsync(file);

        if (!string.IsNullOrEmpty(path))
        {
            Anime.TrailerUrl = path; // Update DTO
            Snackbar.Add("Trailer video uploaded!", Severity.Success);
        }
        else
        {
            Snackbar.Add("Trailer video upload failed.", Severity.Error);
        }

        IsUploadingTrailer = false;
        StateHasChanged();
    }

    private async Task UpsertAnime()
    {
        IsLoading = true;
        bool success = false;

        try
        {
            // Check if a new file has been selected
            if (SelectedFile != null)
            {
                // The service handles the file stream, API call, and oldImageUrl parameter.
                var newImagePath = await AnimeService.UploadImageAsync(SelectedFile, Anime.ImageUrl);

                if (!string.IsNullOrEmpty(newImagePath))
                {
                    // Get the new image path from the API
                    UploadedImageUrl = newImagePath;
                    Anime.ImageUrl = newImagePath;
                }
                else
                {
                    Snackbar.Add("Image upload failed.", Severity.Error);
                    IsLoading = false;
                    return; // Stop the process if image upload fails
                }
            }
            else
            {
                // If no new file, retain the existing ImageUrl from the loaded Anime object
                // This is crucial for updates where the user doesn't change the image
                Anime.ImageUrl = UploadedImageUrl;
            }

            if (Id == 0)
            {
                // Create
                var createDto = new AnimeCreateDto
                {
                    Title = Anime.Title,
                    Description = Anime.Description,
                    Episodes = Anime.Episodes,
                    Season = Anime.Season,
                    PremieredYear = Anime.PremieredYear,
                    ImageUrl = Anime.ImageUrl,
                    TrailerPosterUrl = Anime.TrailerPosterUrl,
                    TrailerUrl = Anime.TrailerUrl,
                    Rating = Anime.Rating,
                    Status = Anime.Status,
                    CategoryId = Anime.CategoryId,
                    GenreIds = Anime.GenreIds,
                    StudioIds = Anime.StudioIds
                };

                success = await AnimeService.CreateAnimeAsync(createDto);
            }
            else
            {
                // Update
                success = await AnimeService.UpdateAnimeAsync(Anime);
            }

            // Handle Result
            if (success)
            {
                Snackbar.Add($"Anime {(Id > 0 ? "updated" : "created")} successfully.", Severity.Success);
                Navigation.NavigateTo("/admin/animes");
            }
            else
            {
                Snackbar.Add($"Failed to {(Id > 0 ? "update" : "create")} anime.", Severity.Error);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"An unexpected error occurred: {ex.Message}", Severity.Error);
        }
        finally
        {
            IsLoading = false;
            Navigation.NavigateTo("/admin/animes");
        }

    }

    private void ToggleGenre(int genreId, bool isChecked)
    {
        if (isChecked)
        {
            if (!Anime.GenreIds.Contains(genreId))
                Anime.GenreIds.Add(genreId);
        }
        else
        {
            Anime.GenreIds.Remove(genreId);
        }
        StateHasChanged();
    }

    private string ConvertStudioIdToName(int id)
    {
        // Find the studio object corresponding to the ID and return its Name.
        // Use a default value if the studio isn't found (e.g., for safety).
        return Studios?.FirstOrDefault(s => s.Id == id)?.Name ?? $"ID {id}";
    }

    private Status? AnimeStatusEnum
    {
        get => Anime.Status.HasValue ? (Status)Anime.Status.Value : null;

        // When the user selects a value (Status), set the DTO's Status (int?)
        // If null is passed (unselected), set the DTO's Status to null.
        set => Anime.Status = value.HasValue ? (int)value.Value : null;
    }

    private string GetEnumDisplayName(Enum enumValue)
    {
        // Use reflection to get the MemberInfo for the enum value
        var memberInfo = enumValue.GetType().GetMember(enumValue.ToString()).FirstOrDefault();

        if (memberInfo == null)
        {
            return enumValue.ToString(); // Fallback to raw string
        }

        // Check if the DisplayAttribute is present
        var displayAttribute = memberInfo.GetCustomAttributes(typeof(DisplayAttribute), false)
                                         .OfType<DisplayAttribute>()
                                         .FirstOrDefault();

        // Return the Name from the DisplayAttribute, or fallback to raw string
        return displayAttribute?.Name ?? enumValue.ToString();
    }

    private async Task OnFileSelected(InputFileChangeEventArgs e)
    {
        SelectedFile = e.File;

        if (SelectedFile == null)
            return;

        if (!SelectedFile.ContentType.StartsWith("image/"))
        {
            Snackbar.Add("Please select a valid image file.", Severity.Warning);
            return;
        }

        if (SelectedFile.Size > 5 * 1024 * 1024)
        {
            Snackbar.Add("File size exceeds 5 MB.", Severity.Warning);
            return;
        }

        // Base 64 Preview
        var buffer = new byte[(int)SelectedFile.Size];
        await SelectedFile.OpenReadStream(5 * 1024 * 1024).ReadAsync(buffer); // 5MB max
        PreviewImageUrl = $"data:{SelectedFile.ContentType};base64,{Convert.ToBase64String(buffer)}";

        UploadedImageUrl = null; // Reset uploaded URL until upload completes
    }
}